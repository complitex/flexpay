package org.flexpay.eirc.persistence.filters;

import org.flexpay.common.persistence.filter.ObjectFilter;

/**
 * Parent service filter marker notifies that only parent services should be taken into account
 */
public class ParentServiceFilterMarker extends ObjectFilter {
}
