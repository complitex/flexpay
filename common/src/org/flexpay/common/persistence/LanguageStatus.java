package org.flexpay.common.persistence;

/**
 * Application language status, do not change the order of existing values, this can
 * impact DB
 */
public enum LanguageStatus {
	Disabled, Enabled
}
